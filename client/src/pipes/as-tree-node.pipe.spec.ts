import { AsTreeNodePipe } from './as-tree-node.pipe';

describe('AsTreeNodePipe', () => {
  it('create an instance', () => {
    const pipe = new AsTreeNodePipe();
    expect(pipe).toBeTruthy();
  });
});
