import { AsNestedTreeNodePipe } from './as-nested-tree-node.pipe';

describe('AsNestedTreeNodePipe', () => {
  it('create an instance', () => {
    const pipe = new AsNestedTreeNodePipe();
    expect(pipe).toBeTruthy();
  });
});
